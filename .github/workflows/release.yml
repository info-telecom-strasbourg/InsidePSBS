name: 'publish'
# on:
#   push:
#     tags:
#       - 'app-v0.*'

on: [push]

jobs:  
  build-ios:
    name: build-ios-job
    uses: ./.github/workflows/ios.yml
    with:
      job: build-ios
    secrets: inherit

  build-android:
    name: build-android-job
    uses: ./.github/workflows/android.yml
    with:
      job: build-android
    secrets: inherit