export type Theme = {
  background: string;
  box: string;
  box_secondary: string;
  text: string;
  text_secondary: string;
  tabBar: string;
  primary: string;
};

export type ColorScheme = "auto" | "light" | "dark" | "classic" | "gold";
