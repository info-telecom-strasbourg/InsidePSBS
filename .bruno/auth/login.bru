meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{API_URL}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "{{USER_EMAIL}}",
    "password": "{{USER_PASSWORD}}"
  }
}

script:post-response {
  bru.setEnvVar("AUTH_TOKEN",res.body.token);
}
